apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aakasha.fullname" . }}-test-connection"
  labels:
    {{- include "aakasha.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aakasha.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
