apiVersion: v1
kind: Service
metadata:
  name: {{ include "aakasha.fullname" . }}
  labels:
    {{- include "aakasha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aakasha.selectorLabels" . | nindent 4 }}
