apiVersion: v1
kind: Service
metadata:
  name: aakasha-frontend
  namespace: {{ .Values.namespace }}

spec:
  type: ClusterIP

  selector:
    app: aakasha-frontend

  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
